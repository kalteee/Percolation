# Percolation
Overview
This project implements a simulation of Bond Percolation on finite square lattices. 

Each edge of the lattice is assigned an independent random paramater e_p uniformly distributed on (0,1). For a given percolation parameter p, we draw the edge if
e_p \leq p. 

